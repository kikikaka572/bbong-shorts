name: 데이터 수집 트리거

on:
  push:
    branches: [main]
  workflow_dispatch:

# Vercel이 GitHub 연동으로 자동 배포하므로 별도 배포 스텝 불필요
# 이 파일은 향후 추가 자동화를 위한 자리 보존용
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: 완료
        run: echo "Vercel이 자동 배포 처리"
